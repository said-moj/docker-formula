{% set docker = salt['grains.filter_by']({
    'Debian': {
        'pkg': 'lxc-docker',
        'pkg_version': '1.6.2',
        'py_version': '1.2.2',
        'datadir': '',
    },
    'default': 'Debian',
}, merge=salt['pillar.get']('docker',{})) %}
